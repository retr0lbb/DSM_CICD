# AULA 5 - GIT

Comandos git que eu fiz

git config --global user.name "retr0lbb"
git config --global user.email "retr0lbb@gmail.com"


git config --global init.defaultBranch main
git config --global color.ui auto

git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.lg "log --oneline --graph --decorate --all"
git config --global alias.undo "reset HEAD~1 --mixed"
git config --global alias.last "log -1 HEAD"

Para mais comandos como pullrequests, issues e voltar a versoes antigas e merge veja o repo (mimer)
// Mudar depois SLK
